"""Хранилка — офлайн-хаб аккаунтов. Корень пакета.

ЕДИНЫЙ ИСТОЧНИК ВЕРСИИ приложения — константа __version__ ниже; при смене
версии правится ТОЛЬКО она. Hranilka.spec читает её при сборке и вшивает в
метаданные Hranilka.exe (Проводник: Свойства → Подробно). Git-тег релиза
ставится вручную тем же номером (v1.2). Формат: "MAJOR.MINOR" или
"MAJOR.MINOR.PATCH" — Windows дополнит нулями до четырёх чисел.

Не путать со SCHEMA_VERSION (hranilka/data/database/schema.py) — это версия схемы БД
для миграций, она меняется независимо."""

__version__ = "1.4"
